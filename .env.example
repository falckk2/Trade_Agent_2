# Copy this file to .env and fill in your BloFin API credentials.
# NEVER commit .env to version control — it is already in .gitignore.
# Rotate keys immediately if they are ever exposed.
BLOFIN_API_KEY=
BLOFIN_SECRET=
BLOFIN_PASSPHRASE=
